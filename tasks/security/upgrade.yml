- name: Install aptitude before performing any bulk upgrades
  apt:
    name: aptitude
    state: present
    update_cache: yes

# - name: Update APT package cache
#   apt:
#     update_cache: yes
#     cache_valid_time: 3600

- name: Upgrade APT to the latest packages
  apt:
    upgrade: safe
